import { createMetadata } from "../../../lib/metadata";

export const metadata = createMetadata({
  title: "Collaboration",
  description:
    "How LogicSoft Technologies collaborates with clients throughout the engagement.",
  path: "/how-we-work/collaboration",
});

export default function Layout({ children }) {
  return children;
}
