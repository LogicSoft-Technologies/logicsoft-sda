import { createMetadata } from "../../../lib/metadata";

export const metadata = createMetadata({
  title: "Reporting",
  description:
    "How LogicSoft Technologies reports project progress to enterprise clients.",
  path: "/how-we-work/reporting",
});

export default function Layout({ children }) {
  return children;
}
